#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
    cin >> n;
    vector<int> heights(n);
    vector<int> cow(n);
    for(int i = 1; i <= n; i++){
        cin >> heights[i-1];
    }	
    for(int i = 1; i <= n; i++){
        cin >> cow[i-1];
    }
    for(int i = 0; i < n; i++){
        for()
    }
}
